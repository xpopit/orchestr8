{
  "description": "orchestr8 plugin hooks - Initialize MCP server on session start",
  "hooks": {
    "SessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 30,
            "description": "Initialize MCP server and orchestr8 environment on session start"
          }
        ]
      }
    ]
  }
}
